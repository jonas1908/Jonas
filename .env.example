# 环境变量模板（可用于本地 .env 或 GitHub Secrets）
#
# - **本地运行**：复制本文件为 `.env`，把等号右侧改成你的真实值
# - **GitHub Actions**：不需要 `.env`，把同名键放到仓库的 Secrets 里即可
#   （Settings → Secrets and variables → Actions → New repository secret）

# ==== Discord 相关 ====
# Discord Bot Token（在这里填写你的 Discord 机器人 Token）
DISCORD_BOT_TOKEN=在这里填写你的 Discord 机器人 Token

# Discord 服务器（Guild）ID（在这里填写你的服务器 ID，纯数字）
DISCORD_GUILD_ID=在这里填写你的 Discord Guild ID（纯数字）

# Discord 频道（Channel）ID（在这里填写要收集建议的频道 ID，纯数字）
DISCORD_CHANNEL_ID=在这里填写你的 Discord Channel ID（纯数字）

# （可选）只收集包含该关键词/前缀的消息，例如 "【建议】"（留空表示不限制）
DISCORD_SUGGESTION_PREFIX=在这里填写“建议帖”前缀（可留空）

# （可选）最多拉取多少条消息用于分析，避免一次拉太多（留空则不限制）
DISCORD_MAX_MESSAGES=在这里填写数字（可留空）

# ==== OpenAI / 大模型相关 ====
# OpenAI API Key（在这里填写你的 OpenAI API Key）
OPENAI_API_KEY=在这里填写你的 OpenAI API Key

# （可选）模型名称，例如 gpt-4.1-mini（不懂就先用默认）
OPENAI_MODEL=gpt-4.1-mini

# （可选）用于打分的标准/偏好（例如更重视“可落地性”）
AI_SCORING_RULE=在这里填写你的打分偏好（可留空）

# ==== 飞书群机器人（Webhook）相关（推荐优先用这个） ====
# 飞书“自定义机器人”的 Webhook 地址，形如：
# https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxx
FEISHU_WEBHOOK=在这里填写你的飞书机器人 Webhook 地址

# （可选）如果你在机器人里开启了“签名校验”，需要填密钥
FEISHU_WEBHOOK_SECRET=在这里填写飞书机器人签名密钥（可留空）

# ==== （可选）飞书多维表格（Bitable）相关 ====
# 如果你未来想把每条建议的分析结果存档到多维表格，再填写下面这些；
# 只做“发周报到群里”的话，可以先不填。

# 飞书自建应用的 App ID（用于获取 tenant access token）
FEISHU_APP_ID=在这里填写你的飞书 App ID（可留空）

# 飞书自建应用的 App Secret
FEISHU_APP_SECRET=在这里填写你的飞书 App Secret（可留空）

# 多维表格所在应用的 App Token（不是 App ID）
FEISHU_BITABLE_APP_TOKEN=在这里填写你的 Bitable App Token（可留空）

# 多维表格的表 ID（Table ID）
FEISHU_BITABLE_TABLE_ID=在这里填写你的 Bitable Table ID（可留空）

# ==== 其它配置 ====
# 时区，例如 Asia/Shanghai
TIMEZONE=Asia/Shanghai

# （可选）每周定时发送的星期与时间（仅用于你的脚本内部展示/日志；真正定时由 GitHub Actions 的 cron 决定）
REPORT_WEEKDAY=MON
REPORT_TIME_LOCAL=10:00

